This folder contains a minimal robotito file system. 

It is automatically updated when update.sh is run.

To flash this file system run the command: export FS_ROOT_PATH=robotito/firmware/fs; make flashfs
